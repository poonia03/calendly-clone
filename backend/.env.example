PORT=4000
CORS_ORIGIN=http://localhost:3000

# Option A: single URL
# DATABASE_URL=postgres://username:password@localhost:5432/calendly_clone

# Option B: discrete variables (used when DATABASE_URL is unset)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=calendly_clone
